{
    "name": "dxgx/tree-size-mailer",
    "description": "Laravel package for generating and emailing directory tree size reports",
    "type": "library",
    "keywords": [
        "laravel",
        "disk-usage",
        "directory-size",
        "email-report",
        "tree-size"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "DeadSimpleApps",
            "email": "deadsimpleapps@gmail.com"
        }
    ],
    "require": {
        "php": "^8.2",
        "illuminate/console": "^9.0|^10.0|^11.0|^12.0",
        "illuminate/mail": "^9.0|^10.0|^11.0|^12.0",
        "illuminate/support": "^9.0|^10.0|^11.0|^12.0"
    },
    "autoload": {
        "psr-4": {
            "DeadSimpleApps\\TreeSizeMailer\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "DeadSimpleApps\\TreeSizeMailer\\TreeSizeMailerServiceProvider"
            ]
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "require-dev": {
        "laravel/pint": "^1.29"
    },
    "scripts": {
        "post-install-cmd": [
            "# Calculate size and remove unnecessary directories from vendor packages to save disk space",
            "find vendor \\( -name '.git' -o -name 'tests' -o -name 'test' -o -name 'demo' -o -name 'examples' -o -name '.github' \\) -type d -exec du -sk {} + 2>/dev/null | awk '{sum+=$1}END{if(sum>0)printf \"Removing %d KB of unnecessary files\\n\",sum}'; find vendor \\( -name '.git' -o -name 'tests' -o -name 'test' -o -name 'demo' -o -name 'examples' -o -name '.github' \\) -type d -prune -exec rm -rf {} + 2>/dev/null; find vendor -name '.DS_Store' -delete 2>/dev/null"
        ],
        "post-update-cmd": [
            "@php artisan vendor:publish --tag=laravel-assets --ansi --force",
            "# Calculate size and remove unnecessary directories from vendor packages to save disk space",
            "find vendor \\( -name '.git' -o -name 'tests' -o -name 'test' -o -name 'demo' -o -name 'examples' -o -name '.github' \\) -type d -exec du -sk {} + 2>/dev/null | awk '{sum+=$1}END{if(sum>0)printf \"Removing %d KB of unnecessary files\\n\",sum}'; find vendor \\( -name '.git' -o -name 'tests' -o -name 'test' -o -name 'demo' -o -name 'examples' -o -name '.github' \\) -type d -prune -exec rm -rf {} + 2>/dev/null; find vendor -name '.DS_Store' -delete 2>/dev/null"
        ]
    }
}
